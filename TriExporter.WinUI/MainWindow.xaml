<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="TriExporter.WinUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:TriExporter.WinUI"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid Grid.Row="0" Background="{ThemeResource SystemControlChromeMediumLowAcrylicWindowMediumBrush}">
            <TextBlock Text="TriExporter WinUI 3" 
                       FontSize="20" 
                       FontWeight="SemiBold" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Margin="12"/>
        </Grid>

        <!-- Main Content Area -->
        <ScrollViewer Grid.Row="1" ZoomMode="Disabled">
            <StackPanel Margin="24" Spacing="16">
                <TextBlock Text="Welcome to TriExporter" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"/>
                
                <TextBlock Text="This is the modern WinUI 3 version of TriExporter." 
                           FontSize="16" 
                           HorizontalAlignment="Center" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" 
                        Padding="16" 
                        Margin="0,24,0,0">
                    <StackPanel Spacing="12">
                        <TextBlock Text="File Operations" 
                                   FontSize="18" 
                                   FontWeight="SemiBold"/>
                        
                        <Button x:Name="OpenFileButton" 
                                Content="Open 3D File" 
                                HorizontalAlignment="Stretch" 
                                Style="{ThemeResource AccentButtonStyle}"
                                Click="OpenFileButton_Click"/>
                        
                        <Button x:Name="ExportFileButton" 
                                Content="Export File" 
                                HorizontalAlignment="Stretch" 
                                IsEnabled="False"
                                Click="ExportFileButton_Click"/>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" 
                        Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="File Information" 
                                   FontSize="18" 
                                   FontWeight="SemiBold"/>
                        
                        <TextBlock x:Name="FileInfoText" 
                                   Text="No file selected" 
                                   FontSize="14" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="2" 
                Background="{ThemeResource SystemControlChromeMediumLowAcrylicWindowMediumBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusText" 
                       Text="Ready" 
                       Margin="12,8" 
                       FontSize="12" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Border>
    </Grid>
</Window>